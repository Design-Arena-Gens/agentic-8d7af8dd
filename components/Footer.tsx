export default function Footer() {
  return (
    <footer id="contact" className="border-t border-white/10 py-10 text-center text-sm text-white/60">
      <div className="container-narrow px-4">
        ? {new Date().getFullYear()} Intelliwave ? Agence de cr?ation de chatbots IA.
      </div>
    </footer>
  )
}
